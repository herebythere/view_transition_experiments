# World tiles

Chrome has trouble with view transitions and javascript modules.



Safari does not seem to have the type modules challenge. Maybe the scripts are loaded before safari is done parsing?
